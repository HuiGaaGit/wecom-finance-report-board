# 17企微财务报表看板：新生产服务器同步部署 ExecPlan

## 目标

以 2026-08-31 已迁移的新生产服务器为唯一生产事实源，保留其 Docker、平台统一认证和 `/platform/finance` 基础路径改造，在此基础上合并本地尚未同步的财务数据简报、营收统计表、顾问投入产出比、上传历史和权限增强等近期改动。最终把本地、GitHub `main`、新服务器源码和生产镜像统一到 `1.1.12`，不覆盖生产 SQLite 数据或上传资料。

## 生产事实源

- 主机：`8.163.36.95`，源码目录 `/data/repos/wecom-finance-report-board`。
- 正式地址：`https://anqiaoyiminxq.com/platform/finance/`。
- 编排：`/data/opt/wecom-finance-report-board/compose.yml`。
- 环境文件：`/data/secrets/wecom-finance-report-board/report-board.env`，不读取或输出真实 Secret。
- 持久数据：`/data/data/wecom-finance-report-board`，独立挂载到 `/var/lib/wecom-finance`。
- 容器：`wecom-finance-report-board`；迁移时线上镜像为 `aqllm/finance-report-board:1.1.11`。
- 认证：`AUTH_MODE=platform`，通过小Q平台身份与成员组接口授权；不再依赖项目 17 自建企微应用 Secret。

## 当前差异

- GitHub `origin/main` 停留在 `6da6518` / `1.1.5`，不是当前完整生产源码。
- 本地工作区基于同一提交，含 `1.1.11` 的近期业务功能和 `62/62` 回归，但仍保留旧企微 OAuth、ZIP/systemd 部署说明。
- 新服务器工作区也基于同一提交，含 Docker、平台认证、生产运维脚本和相应测试；服务器记录显示 `66` 项测试通过。
- 两边均有未提交且重叠的 `app.mjs`、前端、测试、版本及文档改动，必须三方合并，禁止整目录互相覆盖。

## 实施步骤

1. 通过 SSH 只读核对新主机、Git 状态、容器、挂载、端口、健康检查、Compose 和 Nginx；确认数据库与上传目录不在源码目录内。
2. 保存新服务器源码快照，以共同基线 `6da6518` 分别提取本地业务变更和服务器平台化变更，采用三方合并解决重叠。
3. 以服务器平台认证和 Docker 部署为基线，保留本地近期业务模块、权限迁移、前端交互和回归测试；旧自建企微 OAuth/systemd/ZIP 链路仅作为历史记录，不再作为生产操作入口。
4. 统一版本到 `1.1.12`，同步 `package.json`、锁文件、页面可见版本、镜像标签、README、部署说明和发布记录。
5. 本地执行语法检查、部署就绪检查、完整 Node 测试和 Docker tests target；测试集应同时覆盖本地业务功能及服务器平台认证/容器契约，数量不低于服务器迁移前的 66 项。
6. 部署前用容器内备份脚本对生产 SQLite 做一致性备份；构建 `aqllm/finance-report-board:1.1.12`，先验证测试目标和健康检查，再只更新 Compose 镜像并重建应用容器。
7. 验证容器 healthy、`127.0.0.1:3180/api/health`、Nginx、正式地址、平台认证、数据库摘要、默认财务简报顺序和首页首个财务模块跳转。
8. 把最终源码提交并推送 GitHub `main`，随后确认本地、GitHub、新服务器提交和版本一致。

## 安全与回滚

- 不打印、不复制生产环境文件内容；只校验必需键是否存在。
- 不删除或覆盖 `/data/data/wecom-finance-report-board`，数据库和上传资料始终通过独立 bind mount 保留。
- 部署前记录当前镜像摘要、Compose 文件和数据库备份路径；新容器不健康时恢复原镜像标签并重新启动。
- 新服务器生效后不再向 `8.163.95.203` 发布项目 17；旧机内容只保留历史和应急比对用途。

## 验证标准

- `npm run check`、`node --check public/app.js`、`npm test`、`npm run check:deploy` 和 Docker tests target 全部通过。
- 健康接口返回 `version: 1.1.12`、`authMode: platform`。
- 财务数据简报默认位于资产负债表之前，首页“开始浏览报表”进入首个可见财务模块；广宣费只取对应月份科目余额表本期发生额借方并单独列示。
- 营收统计表、顾问投入产出比、上传历史筛选分页、现金净额权限及平台认证回归均保留。
- 生产数据库摘要、上传目录和现有数据在部署前后保持一致，正式地址可正常访问。
- 本地、GitHub 和新服务器 Git 提交一致，生产 Compose 与容器镜像均为 `1.1.12`。

## 执行记录

- 2026-08-31：读取迁移运维文档，确认新服务器为项目 17 的唯一生产目标。
- 2026-08-31：通过现有 ED25519 公钥恢复 `root@8.163.36.95` SSH；主机返回 `iZ7xva3ki6uz7ypevsaf46Z`，容器 `aqllm/finance-report-board:1.1.11` 为 healthy，持久数据挂载独立于源码。
- 2026-08-31：确认本地和服务器均基于 `6da6518` 且各自存在未提交改动，开始三方合并。
- 2026-08-31：以服务器平台认证/Docker 变更为基线完成三方合并；保留本地财务简报、营收统计、顾问投入产出、上传历史和权限增强，冲突集中在 4 个小范围并已逐项复核。
- 2026-08-31：版本统一为 `1.1.12`；本地语法、部署模板检查与合并测试 `69/69` 通过。源码包 `artifacts/wecom-finance-report-board-1.1.12.zip` 的 SHA-256 为 `2C5A6BF52920365912F089507329F09831BF9AC76FFF9195155D5D85993CA481`，服务器上传副本哈希一致。
- 2026-08-31：新服务器 Node 20 容器环境以现有生产依赖层运行合并测试，`69/69` 全部通过。`aqllm/finance-report-board:1.1.12` 隔离冒烟容器使用独立空数据库启动成功，健康接口返回 `version=1.1.12`、`authMode=platform`；生产容器和生产数据尚未切换。
