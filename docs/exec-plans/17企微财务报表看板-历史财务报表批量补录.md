# 历史财务报表批量补录 ExecPlan

## 目标

从 `D:\广州安侨集团\202501财务报表\202501财务报表` 识别各月份、各公司及集团历史报表，只补录生产环境尚未发布的公司、期间和报表类型。重复文件按最后修改时间选择最新版本；所有文件先在隔离数据库中逐月解析、核对数据范围与映射，再经正式上传接口校验和发布。

## 已确认范围

- 来源目录包含 2025-01 至 2026-07 共 19 个月、215 个文件；排除 Office 临时文件、跨月误放副本和明显调整草稿。
- 生产已发布记录按“公司 + 期间 + 报表类型”去重，不重复上传现有数据。
- 去重后识别 55 个主工作簿，其中生产已存在 5 个；另有 5 个独立科目余额表，因此实际待补文件共 55 个：广州月报 14 个、深圳月报 11 个、集团普通合并利润表 11 个、集团营收利润口径 7 个、工资表 7 个、独立科目余额表 5 个。
- 预计形成 147 个报表批次；广州 2025-02 的序时账和科目余额表、2025-03 的科目余额表因源数据错月不发布，2025-04 的科目余额表因源目录缺失不发布。

## 解析修复

1. 兼容早期集团文件把主表命名为通用“利润表”，并仅按管理员明确选择的集团报表类型解析，避免混入公司利润表。
2. 兼容早期工资文件使用 `Sheet1` 等通用工作表名称；按姓名、部门、基本工资和本月提成字段签名选择正确明细表。
3. 早期集团合并文件仅有集团主表、没有公司分表时标记为“无分表可勾稽”并允许保留主表；有公司分表时仍必须逐行加总一致才能发布。
4. 上传汇总文件时校验序时账和科目余额表的实际数据期间；与所选期间不一致的子表从本次批次中排除并返回明确诊断，不让错月数据静默发布。
5. 增加回归测试，使用隔离数据库重新批量解析全部候选文件，并逐月检查报表类型、期间、工资字段、集团子表和勾稽结果。

## 发布流程

1. 在当前稳定源码版本上递增版本号，完成语法、全量测试、部署配置和差异检查。
2. 生成新的发布包并在桌面端、手机端真实浏览器验证上传、预览和历史期间选择。
3. 部署应用修复前先创建生产 SQLite 一致性备份；应用切换和运行时隔离验收通过后，才允许补录数据。
4. 生产上传前再次向用户确认将要传输的 55 个敏感财务工作簿及目标站点。
5. 通过应用上传/发布 API 对应的正常界面工作流写入，不直接修改 SQLite；分批发布并核对最终矩阵、批次数和代表月份。

## 验收标准

- 已发布的公司、月份和报表类型不产生重复批次。
- 60 个候选文件均有明确结果：成功解析、因生产已存在跳过，或因源文件错月/缺失而明确排除具体子表。
- 旧集团合并利润表、营收利润口径表和工资表可以稳定解析；集团合并勾稽不通过时不得发布。
- 错月序时账、科目余额表不会生成可发布批次，上传结果能说明被排除的工作表和原因。
- `node --check app.mjs`、`node --check public/app.js`、`npm run check`、`npm test`、`npm run check:deploy`、`git diff --check` 全部通过。
- 生产发布前后 SQLite `quick_check=ok`，应用健康、容器状态、Nginx 和运行时隔离均通过。

## 回滚

- 应用异常时切回上一稳定镜像和 Compose 快照。
- 数据补录前保留一致性备份；上传批次通过应用现有删除/撤回流程回退，只有数据库完整性失败时才恢复备份。
- 不删除或覆盖任何源文件、旧发布包和历史生产版本。

## 当前完成状态

- 已完成 `1.1.33` 解析修复和 3 组专项回归；全量测试 `93/93` 通过，语法、部署配置和差异检查均通过。
- 已在全新隔离数据库逐一导入 55 个待补文件，生成 147 个已校验上传批次和 147 个快照，SQLite `quick_check=ok`。
- 2025-02 广州序时账及科目余额表、2025-03 广州科目余额表均被识别为 2025-01 数据并排除；其他同文件报表正常保留。
- 2025-08、09 集团文件只有集团主表，标记为无分表可勾稽；2025-10 至 2026-06 所有含公司分表的集团文件逐行勾稽通过。
- 2026-01 至 2026-07 七个月工资表均选中 `Sheet1`，姓名、公司、部门、入职日期、基本工资和本月提成字段完整；有效人员行数依次为 52、53、58、63、72、86、89。
- 桌面真实浏览器已确认历史期间可切换到 2025-01、待处理列表完整显示 147 条，并成功预览 2025-08 通用“利润表”集团主表；页面无整体横向溢出。
- 生产应用修复、正式上传和发布尚未执行，须先完成生产备份，并在上传 55 个敏感财务工作簿前取得用户确认。
