# 财务简报二级项目明细行

## 目标

把 `1.1.27` 的自由文字备注卡片改成一级项目下方的标准二级项目行。每行包含项目名称与金额，列对齐一级项目、字号小一号；继续支持授权人员新增、编辑、删除以及纯文字无空行复制。“营收综合利润”不提供二级项目。

## 约束

- 生产事实源是新服务器 Docker/平台认证链路，不向旧 ECS 发布。
- 保留既有权限键 `module.financial_brief.notes.manage`，避免清空已授权员工。
- 在现有 `financial_brief_notes` 表上增列迁移，不删除或重建生产数据库。
- 只通过应用接口写业务数据；部署前使用容器备份脚本生成 SQLite 一致性备份。

## 实施与验证

- [x] 确认本地、GitHub 与生产稳定版本均为 `1.1.27`。
- [x] 增加二级项目名称、金额兼容字段，并把旧文本迁移为名称。
- [x] 改造接口与界面，禁止“营收综合利润”新增二级项目。
- [x] 更新接口、复制文本、权限和静态界面回归；本地全量回归与并行资产分析测试合计 `90/90`。
- [x] 完成桌面及 `390px` 手机端真实浏览器验收，页面无横向溢出。
- [x] 构建并在服务器 Docker tests 阶段完成全量测试。
- [x] 随资产负债分析交互优化合并为 `1.1.29`，备份生产数据库并验证健康、隔离、数据摘要与公网界面。
- [x] 推送源码并补齐发布记录，确保本地、GitHub、服务器一致。

## 1.1.30 交互修正

- [x] 移除常驻“添加二级项目”文字行，改为一级项目左侧的隐藏浮动 `+`。
- [x] 点击 `+` 后直接展开与一级项目相同三列结构的名称、金额、操作填写行。
- [x] 完成桌面悬停、键盘聚焦、手机端和编辑器列对齐验收；桌面名称/金额输入框左边界分别与一级列精确一致，`390px` 手机端无横向溢出。
- [x] 完成全量回归、生产备份与 `1.1.30` 部署；本地及服务器均为 `90/90`，生产健康与数据完整性检查通过。

## 回滚

部署前保存源码、Compose 与 SQLite 一致性备份。若上线验收失败，恢复前一版 Compose 镜像并重启；只有数据库完整性异常时才考虑使用备份库，禁止覆盖正常业务数据。
